apiVersion: v1
kind: Service
metadata:
  name: verdantnest-service
  labels:
    app: verdantnest
spec:
  type: LoadBalancer
  selector:
    app: verdantnest
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
